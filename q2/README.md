For driving this program with ease on a linux system the following commands 
can be executed.

sudo chmod 777 q2.sh;
./q2.sh

as the .java files needs to be compiled in a sequence this script provides the sequence. 
